#include <iostream>

using namespace std;

int CountPeople(int k, int n) {
  int floor = k;
  int room_num = n;

  for (int i = 0; i < k; i++) {
    
  }
}

int main() {
  int TC;

  cin >> TC;

  for (int i = 0; i < TC; i++) {
    int k, n;
    cin >> k >> n;

    CountPeople(k, n);
  }
}